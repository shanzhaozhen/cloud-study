eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
#      profile: dev
  application:
    name: eureka-client
