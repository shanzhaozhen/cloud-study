eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: api-gateway
      profile: test
  application:
    name: eureka-client